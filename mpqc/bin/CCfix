#!/bin/csh -f

set options = ()
foreach i ($*)
  if ("$i:e" == cc) then
     set filename = `basename $i:r.c++`
     set options = ($options $filename)
     /bin/rm -f $filename
     ln -s $i $filename
  else
     set options = ($options $i)
    endif
  end

CC $options
