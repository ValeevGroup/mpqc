  INSTALL=/bin/mv
  INSTALLBINOPT=
  INSTALLLIBOPT=
ARCH=PARAGON

YACC = bison
LEX = flex

CC = icc
#CXX = ig++ -V 2.4.5
CXX = ig++
FC = if77
AR = ar860
RANLIB = echo
DEFINES := $(DEFINES) -DPARAGON -DI860

#ifeq ($(LD),igcc)
#  LDFLAGS += -mnx
#endif
#ifeq ($(LD),ig++)
#  LDFLAGS += -mnx
#endif
#ifeq ($(LD),icc)
#  LDFLAGS += -nx
#endif
LDFLAGS += -mnx

#vpath %.a
#vpath %.a $(TOPDIR)/src.lib:/lib:/usr/lib:/home/cljanss/gnu/lib

SYSLIBS = -lm -lf

