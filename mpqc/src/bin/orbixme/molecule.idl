
#include <keyval.idl>

interface C_Molecule : C_KeyValCreatable {
  long natom();
  long atomic_number(in long atomnum);
  double x(in long atomnum);
  double y(in long atomnum);
  double z(in long atomnum);
  double r(in long atom1, in long atom2);

  // orbix bug work around
  boolean molecule_has_object();
};
